import { ctrlWrapper } from '../utils/ctrlWrapper.js';

export const addWaterController = ctrlWrapper((req, res) => {});

export const editWaterController = ctrlWrapper((req, res) => {});

export const deleteWaterController = ctrlWrapper((req, res) => {});

export const getWaterController = ctrlWrapper((req, res) => {});
export const getWaterPerMonthController = ctrlWrapper((req, res) => {});
