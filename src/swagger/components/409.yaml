type: object
required:
  - status
  - message
  - data
properties:
  status:
    type: integer
    example: 409
  message:
    description: Message about request
    type: string
    example: User already exist
  data:
    type: object
    required:
      - message
    properties:
      message:
        type: string
        example: 'User already exist'